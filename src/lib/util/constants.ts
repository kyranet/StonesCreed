export const WIN_WIDTH = 800;
export const WIN_HEIGHT = 600;
export const TILE_SIZE = 48;
export const LOADING_HEIGHT = 5;
export const LOADING_POSITION = new Phaser.Point(0, (WIN_HEIGHT / 2) - (LOADING_HEIGHT / 2));
